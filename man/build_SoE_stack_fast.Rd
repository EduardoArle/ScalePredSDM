% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_SoE_stack_fast.R
\name{build_SoE_stack_fast}
\alias{build_SoE_stack_fast}
\title{Build a scale-of-effect (SoE) raster stack from original predictors}
\usage{
build_SoE_stack_fast(
  predictors,
  SoE_table,
  scale_type = c("diameter", "radius"),
  fun_by_pred,
  na.rm = TRUE,
  expand = FALSE,
  filename = NULL,
  overwrite = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{predictors}{SpatRaster. Original predictor layers. Must be named.}

\item{SoE_table}{data.frame or tibble with columns:
\itemize{
\item \code{predictor}: name of the predictor (must match layer names in \code{predictors})
\item \code{scale_m}: numeric scale (map units, typically metres) or \code{"point"}
}}

\item{scale_type}{Character. Interpretation of \code{scale_m}: \code{"diameter"} or \code{"radius"}.}

\item{fun_by_pred}{Named character vector mapping predictors to aggregation
functions (\code{"mean"} or \code{"sum"}). Must include all predictors.}

\item{na.rm}{Logical. Passed to \code{terra::focal()}.}

\item{expand}{Logical. Passed to \code{terra::focal()}.}

\item{filename}{Optional filename to write the resulting raster stack.}

\item{overwrite}{Logical. Overwrite existing file if \code{filename} is supplied.}

\item{quiet}{Logical. If FALSE, prints progress messages.}
}
\value{
SpatRaster with one layer per predictor, named by predictor.
}
\description{
Given (i) an original predictor raster stack and (ii) a table of selected
scales-of-effect, this function produces a new raster stack in which each
predictor is represented at its selected spatial scale.
}
\details{
Spatial aggregation is applied using \code{terra::focal()} for numeric scales.
If \code{scale_m == "point"}, the original predictor raster is returned unchanged.

Aggregation functions must be supplied explicitly via \code{fun_by_pred},
a named character vector mapping each predictor to an aggregation rule
(e.g. \code{"mean"} for continuous variables, \code{"sum"} for compositional or binary variables).
}
