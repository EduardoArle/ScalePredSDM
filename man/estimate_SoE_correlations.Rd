% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_SoE_correlations.R
\name{estimate_SoE_correlations}
\alias{estimate_SoE_correlations}
\title{Estimate scale-of-effect (SoE) correlations for raster predictors}
\usage{
estimate_SoE_correlations(
  predictors,
  points_sf,
  response_col,
  scales_m,
  method = "spearman",
  include_point = TRUE,
  fun_by_pred,
  return_plot = FALSE,
  plot_highlight_best = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{predictors}{SpatRaster. Predictor stack with named layers.}

\item{points_sf}{sf object with POINT geometry.}

\item{response_col}{Character. Column in \code{points_sf} containing the response variable.}

\item{scales_m}{Numeric vector. Buffer radii (map units, typically metres).}

\item{method}{Character. Correlation method passed to \code{stats::cor} (default \code{"spearman"}).}

\item{include_point}{Logical. Include the point scale (\code{"point"}) in the analysis.}

\item{fun_by_pred}{Named character vector mapping predictors to aggregation
functions (\code{"mean"} or \code{"sum"}). Must include all predictors.}

\item{return_plot}{Logical. If TRUE, returns a list including a ggplot object.}

\item{plot_highlight_best}{Logical. Highlight the best (max |correlation|) scale per predictor.}

\item{quiet}{Logical. If FALSE, prints progress messages.}
}
\value{
If \code{return_plot = FALSE}:
A data.frame with columns \code{predictor}, \code{scale}, \code{agg_fun},
\code{correlation}, and \code{n_complete}.

If \code{return_plot = TRUE}:
A list with elements \code{soe_df}, \code{best_scales}, and \code{plot}.
}
\description{
Computes correlations between a response variable measured at point locations
and predictor values extracted at the point scale ("point") and at multiple
spatial buffer scales.
}
\details{
Aggregation functions must be supplied explicitly via \code{fun_by_pred},
a named character vector mapping each predictor to an aggregation rule
(e.g. \code{"mean"} for continuous predictors, \code{"sum"} for compositional or binary predictors).

At the \code{"point"} scale, no spatial aggregation is performed; however, the
aggregation function is still recorded for consistency.
}
